#
# Zodium Project's tuned configuration
#
[main]
summary=Zodium Latency profile - optimized for low latency, with reduced power saving. ( lower throughput for better latency )

[cpu]
force_latency=cstate.id_no_zero:1|3
governor=performance
# energy_perf_bias=performance (legacy)
energy_performance_preference=performance
min_perf_pct=100
boost=1

[acpi]
platform_profile=balanced-performance|performance

[vm]
# The generator of dirty data starts writeback at this percentage (system default
# is 20%)
dirty_bytes=10%

# Start background writeback (via writeback threads) at this percentage (system
# default is 10%)
dirty_background_bytes=3%

[sysctl]
# 0 tells the kernel to avoid swapping processes out of physical memory
# for as long as possible
# 100 tells the kernel to aggressively swap processes out of physical memory
# and move them to swap cache
vm.swappiness=80

[video]
panel_power_savings=0