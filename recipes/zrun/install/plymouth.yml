---
# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

modules:
 - type: dnf
   install:
     install-weak-deps: false
     packages:
      - plymouth
      - plymouth-theme-spinner

 - type: files
   files:
    - source: zodium-plymouth
      destination: /